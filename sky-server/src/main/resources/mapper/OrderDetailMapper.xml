<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.OrderDetailMapper">

    <insert id="insertBatch">
        insert into order_detail (name, image, order_id, dish_id, setmeal_id, dish_flavor, amount,number)
        values
            <foreach collection="orderDetails" item="orderDetail" separator=",">
                   (#{orderDetail.name},#{orderDetail.image},#{orderDetail.orderId},#{orderDetail.dishId},#{orderDetail.setmealId},#{orderDetail.dishFlavor},#{orderDetail.amount},#{orderDetail.number})
            </foreach>

    </insert>
    <select id="getTop10DishName" resultType="java.lang.String">
        select name from order_detail od join orders  o on od.order_id=o.id
            where o.order_time between #{begin} and #{end} group by name order by sum(od.number) desc limit 10

    </select>
    <select id="getTop10DishCount" resultType="java.lang.Integer">
        select sum(od.number) from order_detail od join orders  o on od.order_id=o.id
        where o.order_time between #{begin} and #{end} group by name order by sum(od.number) desc limit 10

    </select>
</mapper>
